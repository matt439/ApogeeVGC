#pragma once

enum class FormatListEntryType
{
	FORMAT_DATA,      // Represents a format data entry
	SECTION_INFO,     // Represents a section info entry
	INVALID,          // Represents an invalid or unrecognized entry type
};